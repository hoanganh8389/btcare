# BizCity Twin AI — Claude Code Project Instructions

> **Precedence (R-AGENT-PARITY):** rule canonical và **tối thượng** cho mọi AI
> coding agent (GitHub Copilot, Claude Code, OpenAI Codex, Cursor) sống tại
> `.github/copilot-instructions.md` và `.github/instructions/*.instructions.md`.
> Các file đó được import nguyên văn bên dưới và đứng trên mọi default của
> Claude Code. Codex nhận cùng bộ rule qua `AGENTS.md` (file sinh tự động).
>
> **Public vs private:** `.github/copilot-instructions.md` là bản công khai cho
> cộng đồng GitHub. Rule vận hành riêng (host, đường dẫn VPS, log path, deploy)
> nằm ở `.github/copilot-instructions.local.md` + `*.local.instructions.md`, được
> `.gitignore` và import qua `CLAUDE.local.md`. Script sync có cổng quét: nếu
> định danh triển khai lọt vào file public, `--check` FAIL.
>
> **Sửa rule ở đúng MỘT nơi:** `.github/`. Không copy rule sang đây; không sửa
> tay `AGENTS.md`, bản đồ môi trường, catalog hay khối marker. Sau khi sửa
> `.github/`, chạy `node bin/sync-agent-instructions.mjs`.
> Spec: `docs/rules/PHASE-0-RULE-AGENT-INSTRUCTION-PARITY.md`.

@.github/copilot-instructions.md

---

## Environment map + path-scoped instructions (áp dụng khi file khớp `applyTo`; `"**"` = mọi file)

<!-- agent-parity:scoped-imports:start (generated by bin/sync-agent-instructions.mjs) -->

@.github/instructions/agent-environment.instructions.md

@.github/instructions/diagnostics-vps-ssh-runbook.instructions.md

<!-- agent-parity:scoped-imports:end -->

---

## Bổ sung riêng cho Claude Code

Không có. Các rule trước đây nằm ở đây (liệt kê file đã sửa sau mỗi slice;
push/deploy VPS luôn phải hỏi và xác nhận) đã chuyển vào R-AGENT-PARITY trong
`.github/copilot-instructions.md` để áp dụng cho mọi agent. Chỉ thêm mục vào
đây khi rule thật sự phụ thuộc tính năng riêng của Claude Code, và không được
nới lỏng rule trong `.github/`.
