# Context Bank Producer Inventory v1

> **ALL CHANNEL - ONE BRAIN**
> 
> Mọi producer trong inventory này mặc định phải đi qua một intake và một
> evidence spine; Context Bank chỉ giữ pointer/correlation, còn canonical owner
> giữ payload và business truth.
> **Stamp:** `[2026-09-02 11:29 AM Johnny Chu - Chu Hoàng Anh]`

> **Status:** CB0.2 inventory baseline / implementation input
> **Date:** 2026-09-01
> **Owner:** Twin AI Core
> **Decision dependency:** [ADR-CB-001](../decisions/ADR-CB-001-CONTEXT-BANK-POINTER-LEDGER.md)
> **Scope:** active producer groups in `bizcity-twin-ai`

This inventory is a routing artifact for CB4 adapters. It records the current
canonical owner and the migration boundary; it does not authorize a producer
to write Context Bank before CB1-CB3 and the relevant runtime probe pass.

## Scope and evidence rules

- Active code only. Paths containing `_archived/`, `_library/`, `vendor/`,
  backup or generated output are excluded.
- A source table/file remains canonical for its own business role. A Context
  Bank row is a pointer/correlation projection and never a payload copy.
- Unknown or migration-incomplete fields are explicitly marked `TBD` or
  `untracked`; they are not inferred from labels, UI state or the current user.
- Account, tenant and identity resolution must happen before Context Bank
  admission. Provider IDs and message content are not safe ledger dimensions.

## Producer matrix

| producer_id | owner path and active event | source zone / canonical truth | tenant + identity resolver | sensitivity / volume / retention | dedupe + event time | candidate role / rollups / KG policy | current probe / adapter | classification / rollback owner |
|---|---|---|---|---|---|---|---|---|
| `crm.message_lifecycle` | `plugins/bizcity-twin-crm/includes/class-repository.php` emits CRM message lifecycle; `plugins/bizcity-twin-crm/includes/class-event-emitter.php` fans out `bizcity_crm_event_crm_message_received` and `..._sent` | Zone 1 customer-care channels: Messenger, Facebook, Zalo OA, Zalo Personal, WebChat and Email. Canonical truth: tenant CRM message/conversation repositories; encrypted archive is the recovery/corpus owner | Tenant: current blog/shard plus exact channel account. Identity: CRM contact/conversation resolver and `BizCity_CRM_Channel_User_Linker`; never a raw provider label | High; message content and contact PII. Expected volume is not frozen and must be measured per tenant; archive policy currently defaults to 365 days in `core/channel-gateway/includes/class-channel-conversation-archive.php` | Provider/account/contact/message identity after canonical normalization; event time from inbound or outbound CRM event, with source timestamp preserved | `event` for lifecycle evidence; optional `memory`/rollup reference only after policy. Conversation state and contact/product affinity are candidates; raw messages are not KG candidates by default | Probe: `core/diagnostics/includes/probes/class-probe-zalo-personal.php` checks archive/listener wiring. Adapter: `class-context-bank-channel-archive-adapter.php` after CB-CH and CB3 | `canonical` CRM lifecycle plus `compatibility_adapter` archive. Rollback: `plugins/bizcity-twin-crm` for CRM event ownership and `core/channel-gateway` for archive |
| `automation.run_lifecycle` | `core/automation/includes/class-automation-repo-runs.php`; workflow run and node decision events are emitted through the existing automation/event infrastructure | Zone 2 when triggered by Zalo Bot/admin commands; otherwise internal workflow engine. Canonical truth: `bizcity_automation_runs` and its approved run/log projections | Tenant: current blog/shard in the repository context. Identity owner is **not proven** because the run schema currently lacks a first-class `user_id` contract; do not use admin/current user fallback | Medium to high; trigger context and model results may contain customer data. Volume and retention are policy/config dependent and require runtime measurement | `run_id` plus parent/root event identity where available; time from run trigger/created timestamp | `event` for run lifecycle; derived run summaries may support automation rollups. KG promotion is selective and must exclude secrets, payment/shipping decisions and raw prompts | Probe: `core/diagnostics/includes/probes/class-probe-automation.php` covers table/basic runtime. Adapter requires owner-scoped run identity and an approved `user_id`/owner migration | `canonical` run lifecycle, `projection` for summaries, currently `untracked` for user-owned Context Bank admission. Rollback: `core/automation` |
| `twinbrain.goal_lifecycle` | `core/twinbrain/includes/class-twinbrain-goal-loop-repository.php`; goal open/progress/close and memory writer dispatch through `BizCity_Twin_Event_Bus::dispatch_v2()` | Internal reasoning and per-user TwinChat/Twin GPT workspace. Canonical truth: `bizcity_twin_event_stream` via Event Bus | Tenant: blog/shard plus event owner. Identity: server-resolved `BizCity_TwinWeb_Identity::current()` / user-bound turn context; group/guest restrictions remain policy gates | Medium-high; user goals, reasoning context and extracted memory. Retention and event rate follow Event Stream policy and must be measured by probe | `event_uuid`, trace and parent event identity; Event Stream `occurred_at` | `event` for goal/decision; optional `memory` and `relation` references. KG promotion only for stable, approved summaries; personal decisions remain private | Probe: `core/diagnostics/includes/probes/class-probe-twinbrain-goal-loop.php` has opt-in E2E coverage. Adapter: Event Stream projection, not a second event writer | `canonical`; no migration adapter for Event Stream persistence. Rollback: `core/twin-core/event-stream` |
| `channel.archive_encrypted_corpus` | `core/channel-gateway/includes/class-channel-conversation-archive.php`; hooks `bizcity_crm_event_crm_message_received/sent/delivery_updated` | Zone 1 conversation archive only; Zone 2 excluded. Canonical truth: encrypted monthly JSONL partition under the registered channel archive contract | Tenant/account/contact HMAC partition; identity is CRM contact/provider mapping after account resolution. Raw provider IDs must not become paths | Critical; full encrypted message body and attachments. Default retention is 365 days; append-only monthly partitions | CRM message/event identity and archive event UUID; source `occurred_at`/created time | Archive entry is an `event`/evidence reference, not a KG fact. Rollups may retain bounded partition integrity metadata; KG promotion is none from archive alone | Probe: `class-probe-zalo-personal.php` archive binding. Adapter required to register the existing archive without rewriting or duplicating it | `canonical` encrypted archive, consumed through `compatibility_adapter`. Rollback: `core/channel-gateway` |
| `commerce.order_lifecycle` | WooCommerce status hooks and existing scheduler/membership projectors, including `core/channel-gateway/includes/class-woo-order-handler.php` and `core/membership/includes/class-membership-woo-projector.php` | WooCommerce is canonical order/payment/product truth; scheduler/CRM rows are projections. Blog-scoped tenant | Tenant: blog/shard. Identity: Woo customer/user mapping, with guest identity policy explicit; no latest-user/order inference | High; payment and customer data. Volume/retention must be read from Woo/scheduler policy, not guessed | Canonical order ID + status event/version; Woo order timestamp | `event` and explicit `relation` references. Customer/product affinity and order lifecycle rollups are candidates; payment secrets and raw billing data are never promoted | Probe: `core/diagnostics/includes/probes/class-probe-membership-woo-projection.php`. Adapter required for Woo-to-Context metadata projection | `projection`; Woo remains canonical. Rollback: WooCommerce/order owner plus `core/channel-gateway` scheduler owner |
| `memory.reference_lifecycle` | `core/memory/includes/class-memory-log.php` and `class-memory-log-projector.php`; legacy SQL sinks are migration debt | Internal memory extraction and notes. Canonical target is a registered encrypted filestore contract per role; legacy `bizcity_memory_*` SQL rows are not new-write targets | Identity must be server-resolved user/tenant scope; anonymous session promotion is policy-gated. Exact cross-tenant policy is **TBD** | Critical; personal preferences, rationale and history. Retention differs by user/episodic/rolling/session/note contract and is **TBD** | Record ID/version or correction/supersession key; extracted/updated time | `memory` references only; no default KG promotion. Rollups are user preference/behavior summaries subject to privacy policy | Dedicated migration probe is **missing**. Adapter required: register filestore contracts, stop new SQL payload writes, preserve bounded rollback reads | `untracked` until filestore contracts, owner, retention and probe are approved. Rollback: `core/knowledge` / `core/intent` owners, each by contract |
| `rules.skill_notebook_reference` | `core/skills/bootstrap.php` registry and Skill/TwinNote/notebook binding surfaces; no direct per-turn payload writer | Internal governed rules/knowledge. Canonical truth: Skill/TwinNote/notebook owner and versioned content | Tenant: blog/shard; identity: owner/skill/notebook ID plus version hash and server authorization | Medium; content may contain private examples or PII. Low-frequency edit lifecycle | Skill/notebook ID + immutable version/hash; edit/publish timestamp | `rule` reference only. KG promotion follows the canonical notebook/KG policy; Context Bank does not copy rule bodies | Probe: `core/diagnostics/includes/probes/class-probe-guru-runtime.php`. Adapter is reference-only and owner-navigation based | `projection`; rollback: `core/skills` for Skill lifecycle and `modules/twinchat` for notebook binding |
| `crm.pipeline_lifecycle` | `plugins/bizcity-twin-crm/includes/pipeline/class-pipeline-run-service.php` emits `crm_pipeline_lifecycle` after stage/exception transitions; adapter: `core/context-bank/includes/class-context-bank-crm-pipeline-adapter.php` | Zone 1 CRM pipeline run lifecycle. Canonical truth: tenant `bizcity_crm_opportunities` + `bizcity_crm_pipeline_deadlines`; Context Bank stores only a receipt-backed pointer/correlation projection | Tenant: current blog/shard enforced by Event Emitter + ledger route evidence. Identity: `contact_id` and pipeline run; no owner user ID or message payload admitted | Medium; bounded lifecycle metadata only. Source SQL/filestore retention follows CRM and registered contract policy | CRM `event_uuid` + `run_id` + event name; `occurred_at` from transition event | `event` pointer/rollup reference; `kg_status=not_candidate` by default. Stable pipeline summaries may be reviewed later, but no raw messages or operational payload promotion | Probe: `core.crm.pipeline_lifecycle` planned; adapter landed and remains feature-gated by `bizcity_context_bank_pipeline_capture_enabled` | `projection` / compatibility adapter; rollback: CRM Run Service for event ownership and core Context Bank for pointer admission |

## Explicit unknowns and gates

| Unknown | Why it blocks CB4 | Required evidence / owner |
|---|---|---|
| Automation run owner identity | `bizcity_automation_runs` does not currently prove a user-owned scope | Add/verify owner contract through R-DCL/R-CR and TwinWeb/automation owners; do not infer admin identity |
| Memory filestore contracts and retention | Current memory SQL paths are migration debt, not a safe Context Bank payload source | Register each role contract, define retention/erasure and add a runtime probe owned by Knowledge/Intent |
| Per-tenant producer rates and peak concurrency | Static estimates cannot justify ledger indexes or worker budgets | CB0.3 fixtures plus bounded runtime counters; owner: each source team |
| Woo guest identity policy | Guest email/customer mapping can become PII leakage or an accidental identity merge | Explicit canonical resolver and two-tenant fixture; owner: Woo/CRM |
| Archive-to-ledger receipt contract | Archive must expose a lock-captured receipt before ledger projection | CB2.3/CB4.2 archive owner and Context Bank ledger owner |

## CB0.2 exit status

The active producer owner map is now recorded, including seven producer groups,
classification and rollback ownership. CB0.2 remains **PARTIAL**, not complete:
producer-wide runtime hooks, rates, two-tenant fixtures, owner approvals and
memory/automation identity gaps remain open. No producer is authorized for CB4
capture solely by appearing in this inventory.
