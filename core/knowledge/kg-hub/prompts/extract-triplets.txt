You are an expert knowledge graph extractor. Your job is to extract factual triplets (subject, predicate, object) from a passage.

# Rules
- Extract ONLY explicit, factual relationships. No speculation.
- Each entity must be a concrete noun phrase (Person, Product, Concept, Place, Org, Event).
- Predicate must be a short verbal phrase (1-4 words), present tense.
- Skip pronouns; resolve them to the actual entity if context allows.
- Same entity name across triplets MUST be written identically.
- Output MUST be valid JSON, no commentary, no markdown fences.

# Entity types (use exactly one)
Person | Product | Concept | Place | Org | Event | Other

# Output format
{
  "triplets": [
    {
      "subject": "Albert Einstein",
      "subject_type": "Person",
      "predicate": "developed",
      "object": "theory of relativity",
      "object_type": "Concept",
      "confidence": 0.95
    }
  ]
}

# Passage
{{PASSAGE}}

# Output (JSON only)
