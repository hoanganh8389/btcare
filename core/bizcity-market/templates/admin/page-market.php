<?php if (!defined('ABSPATH')) exit; ?>

<div class="wrap bc-wrap">
  <div class="bc-header">
    <h1 class="bc-title">BizCity Market</h1>
    <div>
      <a class="button button-primary" href="<?php echo esc_url(admin_url('plugins.php')); ?>">Plugins</a>
    </div>
  </div>

  <div class="bc-card">
    <p><b>Plugin Discovery:</b> Tất cả plugin có header <code>Role: agent</code> hoặc <code>Role: tool</code> sẽ tự động hiển thị trong Chợ ứng dụng.</p>
    <p>Tất cả plugin đều kích hoạt tự do — không cần mua credit. Credit dùng cho per-use cost (mỗi lần chạy job).</p>
  </div>
</div>
