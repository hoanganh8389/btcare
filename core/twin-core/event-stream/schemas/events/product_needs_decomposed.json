{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "bizcity://events/product_needs_decomposed.json",
  "title": "product_needs_decomposed",
  "description": "Emitted when need_solution query is decomposed into required shopping items. PHASE-TWB-PRODUCTS (2026-07-15).",
  "type": "object",
  "required": ["trace_id", "count", "items"],
  "properties": {
    "trace_id": { "type": "string" },
    "count": { "type": "integer", "minimum": 0 },
    "items": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Decomposed need items."
    }
  },
  "additionalProperties": true
}
