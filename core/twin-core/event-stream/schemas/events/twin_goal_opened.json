{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "bizcity://events/twin_goal_opened.json",
  "title": "twin_goal_opened",
  "description": "Initial event-sourced snapshot for one Twin Goal Loop.",
  "type": "object",
  "required": ["goal_id", "session_id", "primary_goal", "status", "completion_score", "state"],
  "properties": {
    "goal_id": { "type": "string", "pattern": "^goal_[A-Za-z0-9_-]+$" },
    "session_id": { "type": "string", "minLength": 1 },
    "primary_goal": { "type": "string", "minLength": 1 },
    "status": { "type": "string", "enum": ["clarifying", "planning", "executing", "verifying", "paused", "blocked"] },
    "completion_score": { "type": "number", "minimum": 0, "maximum": 1 },
    "state": { "type": "object" },
    "surface": { "type": "string" },
    "identity_uuid": { "type": "string" }
  },
  "additionalProperties": true
}
