{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "catalog_id": "fixture-contract-catalog",
  "version": "1.0.0",
  "note": "Minimal fixture catalog. Only the IDs listed here exist, so any other declared contract_id must surface as capability.contract_unknown. Kept deliberately smaller than the canonical catalog at core/twin-core/contracts/schema/public/v1/contract-catalog.json.",
  "contracts": [
    { "id": "tool-io-envelope", "version": "1.0.0" },
    { "id": "channel-payload", "version": "1.1.0" },
    { "id": "output-render", "version": "1.0.0" }
  ]
}